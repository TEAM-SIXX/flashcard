// ─── MASTER Q&A DATABASE ─────────────────────────────────────────────────────
// Extend this array to scale to 50,000 cards.
// Shape: { id: number, tech: string, q: string, a: string }

export const MASTER_QA_DB = [
  // ── REACT ──
  { id: 1, tech: "React", q: "What is the Virtual DOM and how does React use it?", a: "The Virtual DOM is a lightweight in-memory representation of the real DOM. React diffs the current vs. previous VDOM trees, computes the minimum set of changes (reconciliation), then batches those to the real DOM — avoiding expensive direct manipulation on every state change." },
  { id: 2, tech: "React", q: "Explain the difference between useEffect and useLayoutEffect.", a: "useEffect fires asynchronously after the browser paints, making it suitable for data fetching and subscriptions. useLayoutEffect fires synchronously before the paint, used when you need to read or mutate the DOM before the user sees it (e.g. measuring element size)." },
  { id: 3, tech: "React", q: "What are React Server Components and when would you use them?", a: "RSCs run only on the server, produce zero client-side JS, and can directly access databases/file systems. Use them for data-heavy, non-interactive UI. Pair with Client Components only where interactivity is needed." },
  { id: 4, tech: "React", q: "How does React Fiber improve on the old stack reconciler?", a: "Fiber breaks rendering work into small units that can be paused, aborted, or reused. This enables concurrent features like time-slicing, Suspense, and priority-based scheduling — preventing long render cycles from blocking the main thread." },
  { id: 5, tech: "React", q: "What is the Context API and what are its performance pitfalls?", a: "Context lets you broadcast state without prop-drilling. The pitfall: every consumer re-renders when context value changes, even if they don't use the changed property. Mitigate by splitting contexts, memoizing the provider value, or using Zustand/Jotai for fine-grained subscriptions." },
  { id: 6, tech: "React", q: "What is useMemo and when should you avoid it?", a: "useMemo caches a computed value between renders. Avoid it for cheap computations — the memoization overhead can exceed the savings. Only use it when a calculation is genuinely expensive or when referential equality matters for downstream component props." },
  { id: 7, tech: "React", q: "Explain React's reconciliation algorithm.", a: "React compares trees by type first — different types trigger full subtree replacement. For same-type elements it updates props in place. Lists use the key prop to match old vs new children, avoiding O(n³) comparison by assuming same-level keys are unique." },
  // ── NODE.JS ──
  { id: 8, tech: "Node.js", q: "How does the Node.js event loop work?", a: "Node runs on a single thread. The event loop cycles through phases: timers → I/O callbacks → idle → poll → check (setImmediate) → close callbacks. Async work is delegated to libuv's thread pool; callbacks are queued and processed between loop ticks." },
  { id: 9, tech: "Node.js", q: "What is the difference between process.nextTick and setImmediate?", a: "process.nextTick callbacks execute at the end of the current operation, before the event loop continues — higher priority. setImmediate callbacks execute in the check phase, after I/O events. Overusing nextTick can starve the event loop." },
  { id: 10, tech: "Node.js", q: "How would you handle CPU-intensive tasks in Node.js?", a: "Offload to Worker Threads (worker_threads module), child processes (child_process.fork), or external services. Never block the main event loop with heavy computation — it prevents handling concurrent requests." },
  { id: 11, tech: "Node.js", q: "Explain Streams in Node.js and give a production use case.", a: "Streams process data in chunks rather than loading it all into memory. Use case: piping a 10GB CSV from S3 through a Transform stream that parses/filters rows directly into a database write stream — constant memory footprint regardless of file size." },
  { id: 12, tech: "Node.js", q: "How do you prevent memory leaks in a long-running Node.js service?", a: "Common culprits: unremoved event listeners, growing caches without TTL, closures holding references, unbounded queues. Use --inspect + Chrome DevTools heap snapshots to find leaks. Use WeakMap/WeakRef for caches, always remove listeners on cleanup." },
  // ── TYPESCRIPT ──
  { id: 13, tech: "TypeScript", q: "What is the difference between interface and type in TypeScript?", a: "Interfaces are extendable via declaration merging and are best for object shapes. Types can represent unions, intersections, mapped types, and primitives — more expressive. Use interface for public API shapes, type for complex compositions." },
  { id: 14, tech: "TypeScript", q: "What are conditional types and give a real-world example?", a: "Conditional types let the type system branch: T extends U ? X : Y. Example: type Awaited<T> = T extends Promise<infer R> ? R : T — unwraps a Promise's resolved value, used throughout TS's stdlib for async inference." },
  { id: 15, tech: "TypeScript", q: "Explain the infer keyword.", a: "infer captures a type variable within a conditional type's extends clause. Example: type ReturnType<T> = T extends (...args: any[]) => infer R ? R : never — R is inferred from whatever the function returns without explicitly passing it." },
  { id: 16, tech: "TypeScript", q: "What is the difference between unknown and any?", a: "any disables type checking entirely. unknown is the safe alternative: you must narrow it (typeof, instanceof, type guard) before operating on it. Prefer unknown for values of truly unknown shape." },
  { id: 17, tech: "TypeScript", q: "How do mapped types work?", a: "Mapped types iterate over a union of keys to produce a new type. Example: type Partial<T> = { [K in keyof T]?: T[K] } — makes every property optional. Combine with as clauses for key remapping and conditional types for value transformation." },
  // ── PYTHON ──
  { id: 18, tech: "Python", q: "Explain Python's GIL and its impact on multi-threading.", a: "The GIL allows only one thread to execute Python bytecode at a time, limiting CPU parallelism in threads. For CPU-bound work, use multiprocessing or C extensions. For I/O-bound work, threads or asyncio are effective because I/O releases the GIL." },
  { id: 19, tech: "Python", q: "What are Python generators and when should you use them?", a: "Generators are functions using yield that lazily produce values one at a time. Use them for large sequences (reading huge files line by line), infinite streams, or pipelines where you don't need all values in memory at once." },
  { id: 20, tech: "Python", q: "How does Python's asyncio event loop differ from Node.js?", a: "Both are single-threaded async loops, but Python's is opt-in (you must use async/await explicitly and run asyncio.run()). Python also has loop.run_in_executor for running blocking calls without blocking the event loop." },
  // ── POSTGRESQL ──
  { id: 21, tech: "PostgreSQL", q: "Explain the difference between B-tree and GIN indexes.", a: "B-tree indexes are the default, optimized for equality and range queries on scalar values. GIN is for composite values like arrays, JSONB, and full-text search — indexing each element within the value. Wrong index type = table scans at scale." },
  { id: 22, tech: "PostgreSQL", q: "What is MVCC and how does PostgreSQL implement it?", a: "Multi-Version Concurrency Control lets readers and writers not block each other. PostgreSQL stores multiple row versions (tuples) with xmin/xmax transaction IDs. A query sees the snapshot valid at its transaction start. Dead tuples are cleaned up by VACUUM." },
  { id: 23, tech: "PostgreSQL", q: "How would you diagnose and fix a slow query in production?", a: "1) EXPLAIN ANALYZE to see actual vs estimated rows and seq scans. 2) Check for missing indexes. 3) Look at table statistics — run ANALYZE if stale. 4) Rewrite with CTEs. 5) Consider partial indexes, covering indexes, or partitioning for large tables." },
  // ── AWS ──
  { id: 24, tech: "AWS", q: "What is the difference between SQS Standard and FIFO queues?", a: "Standard queues offer at-least-once delivery, best-effort ordering, and nearly unlimited throughput. FIFO queues guarantee exactly-once processing and strict ordering, but cap at 3,000 messages/sec with batching." },
  { id: 25, tech: "AWS", q: "Explain the difference between ECS and EKS.", a: "ECS is AWS's proprietary container orchestrator — simpler, tightly integrated, less operational overhead. EKS is managed Kubernetes — portable across clouds, full Kubernetes API, more complexity. Choose ECS for AWS-native simplicity; EKS for portability." },
  { id: 26, tech: "AWS", q: "How does S3 achieve durability and how would you architect disaster recovery?", a: "S3 Standard replicates objects across ≥3 AZs, achieving 99.999999999% durability. For DR: enable Cross-Region Replication, use S3 Versioning to protect against accidental deletes, set lifecycle policies to archive to Glacier." },
  { id: 27, tech: "AWS", q: "What is the difference between ALB and NLB?", a: "ALB (Application Load Balancer) operates at Layer 7 — understands HTTP/HTTPS, supports path/host-based routing, WebSockets, gRPC. NLB (Network Load Balancer) operates at Layer 4 — ultra-low latency, handles millions of requests/sec, preserves source IP, ideal for TCP/UDP." },
  // ── DOCKER ──
  { id: 28, tech: "Docker", q: "Explain Docker's layer caching and how to optimize Dockerfile builds.", a: "Each instruction creates a layer; unchanged layers are cached and reused. Optimization: order instructions from least to most frequently changed — copy package.json and run npm install before copying source code. Dependency layers stay cached across code changes." },
  { id: 29, tech: "Docker", q: "What is the difference between CMD and ENTRYPOINT?", a: "ENTRYPOINT defines the executable that always runs — the main process. CMD provides default arguments to ENTRYPOINT. When both are set: docker run myimage --flag passes --flag as an argument to ENTRYPOINT, replacing CMD defaults." },
  { id: 30, tech: "Docker", q: "What are multi-stage builds and why use them?", a: "Multi-stage builds use multiple FROM instructions in one Dockerfile. Earlier stages compile/build; the final stage copies only the artifacts needed to run. Result: production images with no build tools, compilers, or dev dependencies — dramatically smaller and more secure." },
  // ── KUBERNETES ──
  { id: 31, tech: "Kubernetes", q: "What is the difference between a Deployment and a StatefulSet?", a: "Deployments manage stateless pods — pods are interchangeable and can be rescheduled anywhere. StatefulSets provide stable network identities, ordered deployment/scaling, and persistent storage per pod. Use StatefulSets for databases and queues." },
  { id: 32, tech: "Kubernetes", q: "How do Kubernetes resource requests and limits work?", a: "Requests are what the scheduler uses to find a node with enough capacity — the pod is guaranteed that amount. Limits cap what the container can use; exceeding CPU limits causes throttling, exceeding memory limits kills the container (OOMKilled)." },
  { id: 33, tech: "Kubernetes", q: "Explain Kubernetes liveness vs readiness probes.", a: "Liveness probes detect if a container is stuck/deadlocked and restart it. Readiness probes detect if a container is ready to serve traffic — if it fails, the pod is removed from Service endpoints without being restarted. Both are critical for zero-downtime deployments." },
  // ── REDIS ──
  { id: 34, tech: "Redis", q: "What persistence options does Redis offer?", a: "RDB: point-in-time snapshots at intervals — fast restarts, risk of data loss since last snapshot. AOF: logs every write — durable but slower. AOF + fsync every second is the production sweet spot. For pure caches (rebuildable data), disable persistence for max performance." },
  { id: 35, tech: "Redis", q: "Explain Redis Cluster and its key sharding model.", a: "Redis Cluster splits the key space into 16,384 hash slots distributed across nodes. Each key is assigned a slot via CRC16(key) % 16384. Clients use MOVED redirects or cluster-aware libraries to route requests to the correct node." },
  { id: 36, tech: "Redis", q: "What is the difference between Redis WATCH and transactions?", a: "MULTI/EXEC provides a transaction that queues commands and executes them atomically. WATCH adds optimistic locking — if a watched key changes between WATCH and EXEC, the transaction aborts. For complex atomic operations, Lua scripts via EVAL are more reliable." },
  // ── GRAPHQL ──
  { id: 37, tech: "GraphQL", q: "What is the N+1 problem in GraphQL and how do you solve it?", a: "When resolving a list of items each with a sub-field (e.g. 100 users, each fetching their org), naively you fire 1 + 100 queries. Solution: DataLoader — it batches all IDs collected in a single tick into one query, then distributes results back to each resolver." },
  { id: 38, tech: "GraphQL", q: "What are GraphQL subscriptions and how are they implemented?", a: "Subscriptions deliver real-time updates over a persistent connection (WebSocket or SSE). The server publishes events to a PubSub channel; the client's subscription resolver listens and pushes relevant events. Apollo Server + graphql-ws is the standard stack." },
  // ── SQL ──
  { id: 39, tech: "SQL", q: "What is the difference between INNER JOIN, LEFT JOIN, and FULL OUTER JOIN?", a: "INNER JOIN returns rows where both tables have matching keys. LEFT JOIN returns all rows from the left table and matched rows from the right (NULLs where no match). FULL OUTER JOIN returns all rows from both tables, with NULLs wherever there's no match on either side." },
  { id: 40, tech: "SQL", q: "How do window functions differ from GROUP BY?", a: "GROUP BY collapses rows into one per group, removing detail. Window functions (ROW_NUMBER, LAG, RANK, SUM OVER) compute across a defined partition of rows while preserving all original rows — letting you compare a row to its group without aggregating." },
  // ── NEXT.JS ──
  { id: 41, tech: "Next.js", q: "Explain the difference between SSR, SSG, and ISR in Next.js.", a: "SSR renders on every request — fresh data, higher server load. SSG pre-builds at deploy time — fastest, but stale until rebuild. ISR revalidates pages in the background on a time or on-demand trigger — combines the speed of SSG with acceptable freshness." },
  { id: 42, tech: "Next.js", q: "How does the App Router differ from the Pages Router?", a: "App Router uses React Server Components by default, enabling server-side data fetching per component without extra API routes. It introduces nested layouts, parallel routes, and intercepting routes. Pages Router is the legacy system using getServerSideProps/getStaticProps." },
  // ── MONGODB ──
  { id: 43, tech: "MongoDB", q: "When would you embed a document vs reference another collection?", a: "Embed when data is accessed together frequently, has a 1-to-few relationship, and rarely changes independently. Reference when data is shared across many documents, grows unboundedly, or must be queried independently. Embedding favors reads; referencing favors write flexibility." },
  { id: 44, tech: "MongoDB", q: "What is the aggregation pipeline?", a: "A sequence of stages that transform documents: $match (filter), $group (aggregate), $project (reshape), $lookup (join), $unwind (flatten arrays), $sort, $limit. Each stage passes its output to the next. More expressive than MapReduce for analytics." },
  // ── REST API ──
  { id: 45, tech: "REST API", q: "What makes an API truly RESTful? What are the 6 constraints?", a: "1) Client-Server separation. 2) Statelessness. 3) Cacheability. 4) Uniform Interface — consistent resource identifiers. 5) Layered System — client can't tell if it's hitting origin or proxy. 6) Code on Demand (optional) — server can send executable code." },
  { id: 46, tech: "REST API", q: "How would you version a REST API without breaking existing clients?", a: "URI versioning (/v1/users) is most explicit. Header versioning (Accept: application/vnd.api.v2+json) keeps URLs clean but is harder to test. Always support the previous major version for a migration window and send deprecation headers." },
  // ── SYSTEM DESIGN ──
  { id: 47, tech: "System Design", q: "How would you design a rate limiter?", a: "Token bucket or sliding window log algorithms. Store counts in Redis with TTL. For distributed systems, use Redis Lua scripts for atomic increment + expire. Key by user ID + endpoint. Return 429 with Retry-After header. Deploy at the edge for lowest latency." },
  { id: 48, tech: "System Design", q: "How would you design a URL shortener at scale?", a: "Generate a unique 6-char base62 ID (hash or auto-increment + base62 encoding). Store in a DB with original URL + metadata. Cache hot mappings in Redis. Use a CDN to handle redirect traffic. Push click events to a Kafka stream for async analytics." },
  { id: 49, tech: "System Design", q: "How do you design for idempotency in distributed systems?", a: "Assign client-generated idempotency keys (UUID) to mutations. Server stores (key → result) in a durable store with TTL. On duplicate request, return cached result without re-executing. Critical for payments, email sends, and any side-effect-producing API." },
  { id: 50, tech: "System Design", q: "What is the CAP theorem?", a: "In a distributed system, you can only guarantee 2 of 3: Consistency (all nodes see same data), Availability (every request gets a response), Partition Tolerance (system works despite network splits). Real systems choose CP (PostgreSQL) or AP (Cassandra, DynamoDB eventual mode)." },
  // ── SECURITY ──
  { id: 51, tech: "Security", q: "Explain SQL injection and how to prevent it.", a: "SQL injection inserts malicious SQL via user input that gets executed by the database. Prevention: always use parameterized queries / prepared statements — never concatenate user input into SQL. Also: least-privilege DB users, WAF as defence-in-depth." },
  { id: 52, tech: "Security", q: "What is JWT and what are its security pitfalls?", a: "JWTs are self-contained, signed tokens. Pitfalls: 1) alg:none attack — validate algorithm explicitly. 2) localStorage exposes to XSS — prefer httpOnly cookies. 3) No built-in revocation — use short expiry + refresh token rotation. 4) Payload is base64, not encrypted — use JWE if needed." },
  { id: 53, tech: "Security", q: "What is CSRF and how do you prevent it?", a: "CSRF tricks authenticated users into submitting requests to another site. Prevention: SameSite=Strict/Lax cookies (modern standard), CSRF tokens in forms (synchronizer token pattern), verifying Origin/Referer headers. SameSite cookies alone cover most cases in modern browsers." },
  // ── ALGORITHMS ──
  { id: 54, tech: "Algorithms", q: "Explain the time complexity of quicksort and when it degrades.", a: "Average O(n log n) with good pivot selection. Worst case O(n²) when pivot is always the min or max. Mitigate with randomized pivot or median-of-three. Introsort (used in most stdlib implementations) falls back to heapsort to guarantee O(n log n)." },
  { id: 55, tech: "Algorithms", q: "What is dynamic programming? Explain with an example.", a: "DP solves problems by breaking them into overlapping subproblems and storing results to avoid recomputation. Example: Fibonacci — naive recursion is O(2ⁿ), memoized is O(n). Coin change: dp[amount] = min coins to make that amount, built bottom-up from dp[0]=0." },
  { id: 56, tech: "Algorithms", q: "Explain BFS vs DFS and when to use each.", a: "BFS explores level by level using a queue — optimal for shortest path in unweighted graphs, finding nodes at minimum distance. DFS explores depth-first using a stack (or recursion) — better for cycle detection, topological sort, connected components, and exhaustive search." },
  // ── CI/CD ──
  { id: 57, tech: "CI/CD", q: "What is the difference between continuous delivery and continuous deployment?", a: "Continuous Delivery means every change is built, tested, and ready to deploy at the push of a button — deployment is manual. Continuous Deployment means every passing change is automatically deployed to production with no human gate." },
  { id: 58, tech: "CI/CD", q: "How would you design a zero-downtime deployment pipeline?", a: "Use blue-green (maintain two identical environments, switch traffic atomically) or canary (gradually shift traffic, roll back on error spikes). Combine with feature flags. Database migrations must be backward-compatible (expand-contract pattern) so both versions run simultaneously." },
  // ── EXPRESS ──
  { id: 59, tech: "Express", q: "How does Express middleware work and what is the execution order?", a: "Middleware functions are called in the order they are defined. Each receives (req, res, next) — call next() to pass control to the next middleware, or end the cycle by sending a response. Error middleware takes 4 params (err, req, res, next) and must be defined last." },
  { id: 60, tech: "Express", q: "How would you structure a large Express application?", a: "Feature-based folder structure: /routes, /controllers, /services, /models, /middleware. Controllers handle HTTP, services contain business logic, models handle data access. Use a router per domain (userRouter, orderRouter) mounted in app.js. Separate the Express app from the server for testability." },
];

export const ALL_TECHS = [...new Set(MASTER_QA_DB.map(q => q.tech))];
